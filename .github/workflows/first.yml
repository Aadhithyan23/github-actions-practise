name: First Workflow

on: push

jobs:
    hello:
        runs-on: ubuntu-latest

        steps:

            - run: pwd
            - run: node --version
            - run: echo "I am learning Github actions"

